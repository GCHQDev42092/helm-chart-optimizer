# Helm Chart Optimizer

Automates the tuning of Helm charts via rule-based and ML-driven approaches.

## Getting Started

```bash
git clone ...
cd helm-chart-optimizer
# Build CLI
go build -o bin/optimizer ./cmd/optimizer